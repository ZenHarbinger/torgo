machine:
  java:
    version: oraclejdk8
test:
  override:
    - mvn -DskipTests=false clean cobertura:cobertura
  post:
    - bash <(curl -s https://codecov.io/bash)
#general:
#  branches:
#    only:
#     - java8
#     - master
#     - tests
#     - /feature-.*/ # or regexes
