machine:
  java:
    version: oraclejdk8
test:
  post:
    - bash <(curl -s https://codecov.io/bash)
general:
  branches:
    only:
     - java8
     - master
     - tests
#     - /feature-.*/ # or regexes
