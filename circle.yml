machine:
  java:
    version: oraclejdk8
test:
  post:
#    - mvn clean cobertura:cobertura coveralls:report
    - bash <(curl -s https://codecov.io/bash)
general:
  branches:
    only:
     - java8
     - master
     - tests
#     - /feature-.*/ # or regexes
